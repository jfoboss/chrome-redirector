{
  "name": "chrome-redirector",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test test/*.test.mjs",
    "build": "scripts/build.sh",
    "store-images": "node scripts/store-images.mjs"
  },
  "devDependencies": {
    "playwright": "^1.56.1"
  }
}
